from django.conf import settings
from django.conf.urls import url,static
from ebay import views

# urlpatterns = [
    # url(r'^api/crawl/', views.crawl, name='crawl'),
# ]

# from django.urls import path

# from . import views

# urlpatterns = [
#     path('', views.index, name='index'),
# ]